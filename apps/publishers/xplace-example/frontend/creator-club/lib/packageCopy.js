import { buildMonetizationPackagePresentation } from "../../../../../../packages/browser-host/src/xms.ts";

export function buildPackageCopy(item) {
  return buildMonetizationPackagePresentation(item);
}
